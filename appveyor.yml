version: 0.1.0.{build}-{branch}

skip_commits:
  message: "([A|a]dd(ed)?|[U|u]pdate(d)?|[Ff]ix(e[sd])?) ([R|r]eadme|[L|l]icense|[C|c]omment)" # One day I'll figure out case-insensitive patterns

build: false

test_script:
  - ps: . .\Build\build.ps1 -Task "Test" -Bootstrap